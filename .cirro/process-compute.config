
aws {
  region = 'us-east-1'
}

process {
  executor = 'awsbatch'

  withName: MERGE_ALL_BAMS {
    errorStrategy = 'terminate'
    maxRetries = 2
    cpus   = 1
    memory = { 32.GB * task.attempt } 
  }
}